#pragma once

#include <algorithm>
#include <map>
#include <memory>
#include <set>
#include <fstream>

#include <logger.h>

#define GLFW_INCLUDE_VULKAN
#include <GLFW/glfw3.h>

namespace {
// Todo: Replace these with settings
const int WIDTH = 800;
const int HEIGHT = 600;
}  // namespace